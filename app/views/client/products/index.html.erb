<% @products.each do |product| %>

<h1><a href="/client/products/<%= product["id"] %>"><%= product["name"] %></a></h1>
<h5>Price :<%= product["price"] %></h5>
<p>IDescription:<%= product["description"] %><p>

<% end %>
